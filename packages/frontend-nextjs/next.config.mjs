/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['i.pravatar.cc', 'picsum.photos'], // Adicionando os domínios permitidos
    },
  };
  
  export default nextConfig;
  